def gcd(a, b):
    while b:
        a, b = b, a % b
    return a

def lcm(x, y):
    return (x * y) // gcd(x, y)

# Example usage:
num1 = 12
num2 = 15
print(f"LCM of {num1} and {num2} is {lcm(num1, num2)}")
